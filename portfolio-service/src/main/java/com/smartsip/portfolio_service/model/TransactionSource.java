package com.smartsip.portfolio_service.model;

public enum TransactionSource {
    MANUAL,     // User entered manually
    EXCEL,      // Imported from Excel
    API         // From broker API (future)
}